// test/widget_test.dart

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Basic test - always passes', () {
    expect(true, true);
  });

  test('Math test - always passes', () {
    expect(1 + 1, 2);
  });
}