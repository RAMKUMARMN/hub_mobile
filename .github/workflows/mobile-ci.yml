name: Mobile CI

on:
  push:
    branches: [main, dev, cg2]
    paths:
      - 'lib/**'              # Dart code
      - 'android/**'          # Android native code
      - 'ios/**'              # iOS native code (if exists)
      - 'pubspec.yaml'        # Dependencies
      - 'pubspec.lock'        # Lock file
      - '.github/workflows/mobile-ci.yml'
  pull_request:
    branches: [main, dev, cg2]
    paths:
      - 'lib/**'
      - 'android/**'
      - 'ios/**'
      - 'pubspec.yaml'
      - 'pubspec.lock'
      - '.github/workflows/mobile-ci.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze-and-test:
    name: Analyze & Test
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Verify Flutter installation
        run: flutter --version

      - name: Create .env file for CI
        run: |
          echo "API_URL=http://localhost:8000" > .env
          echo "ENVIRONMENT=test" >> .env

      - name: Decode google-services.json
        run: |
          echo "${{ secrets.GOOGLE_SERVICES_JSON }}" | base64 -d > android/app/google-services.json

      - name: Decode google-services.json (debug)
        run: |
          mkdir -p android/app/src/debug
          cp android/app/google-services.json android/app/src/debug/

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze code
        run: flutter analyze
        continue-on-error: true

      - name: Run tests (if any)
        run: flutter test --no-pub
        continue-on-error: true

      - name: Build Debug APK
        run: flutter build apk --debug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: build/app/outputs/flutter-apk/app-debug.apk
          retention-days: 7

      - name: Check secrets (debug only)
        run: |
          if [ -n "${{ secrets.KEYSTORE_BASE64 }}" ]; then
            echo "Keystore secret exists"
          else
            echo "Keystore secret NOT found - release builds will be skipped"
          fi