// lib/themes/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  /// DARK THEME COLORS
  static const background = Color(0xFF171E3B);
  static const surface = Color(0xFF113182);
  static const primaryBlue = Color(0xFF0080FF);
  static const aiCyan = Color(0xFF0BD1FA);
  static const accentPurple = Color(0xFFA489F7);

  /// LIGHT THEME COLORS
  static const lightBackground = Color(0xFFF5F7FF);
  static const lightSurface = Colors.white;
  static const lightText = Color(0xFF171E3B);

  /// GLASSMORPHISM COLORS
  static const glassDark = Color(0x1AFFFFFF);      // 10% white
  static const glassLight = Color(0x1A000000);     // 10% black
  static const glassBorder = Color(0x33FFFFFF);    // 20% white
  static const glassBorderLight = Color(0x1A000000); // 10% black

  /// COMMON
  static const white = Colors.white;
  static const black = Colors.black;
  static const grey = Colors.grey;
}